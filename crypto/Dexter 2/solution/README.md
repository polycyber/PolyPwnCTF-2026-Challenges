# Nom du défi
DexXOR's Lab

## Write-up

Using the self-inverse property of XOR, it's possible to isolate the flag by finding the hex of all 4 keys.

En utilisant la propriété d'auto-inverse de XOR, il est possible d'isoler le flag en trouvant le hex des 4 clés.

## Flag

`POLYCYBER{0m3letTe_Du_X0r}`